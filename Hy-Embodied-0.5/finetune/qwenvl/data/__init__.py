"""
qwenvl.data package.

This fine-tuning code expects the user to supply their own dataset as a JSON or
JSONL file (or a directory of "splits"). pass the JSON
file path directly via ``--dataset_use``.
"""
